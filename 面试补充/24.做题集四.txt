2.49  判断对错：已知x = 3，那么赋值语句x = 'abcedfg'是无法正常执行的。 0
2.50  判断对错：Python变量使用前必须先声明，并且一旦声明就不能在当前作用域内改变其类型了。0
2.51  判断对错：Python不允许使用关键字作为变量名，但是允许使用内置函数名作为变量名，不过这会改变函数名的含义，所以不建议这样做。 1
2.52  判断对错：在Python中可以使用if作为变量名。0
2.53  判断对错：在Python 3.x中可以使用中文作为变量名。1
2.54  判断对错：Python变量名必须以字母或下划线开头，并且区分字母大小写。1
2.55  判断对错：加法运算符可以用来连接字符串并生成新字符串。1
2.56  判断对错：x = 9999**9999这样的语句在Python中无法运行，因为数字太大了超出了整型变量的表示范围。0
2.57  判断对错：3+4j不是合法的Python表达式。0
2.58  判断对错：0o12f是合法的八进制数字。0
2.59  判断对错：只有Python扩展库才需要导入以后才能使用其中的对象，Python标准库不需要导入即可使用其中的所有对象。0
2.60  判断对错：在Python中0xad是合法的十六进制数字表示形式。1
2.61  判断对错：4j 是合法Python数字类型。1
2.62  判断对错：Python使用缩进来体现代码之间的逻辑关系，对缩进的要求非常严格。1
2.63  判断对错：Python代码的注释只有一种方式，那就是使用#符号。0
2.64  判断对错：放在一对三引号之间的任何内容将被认为是注释。0
2.65  判断对错：尽管可以使用import语句一次导入任意多个标准库或扩展库，但是仍建议每次只导入一个标准库或扩展库。1
2.66  判断对错：为了让代码更加紧凑，编写Python程序时应尽量避免加入空格和空行。0
2.67  判断对错：在Python 3.5中运算符+不仅可以实现数值的相加、字符串连接，还可以实现列表、元组的连接和集合的并集运算。0
2.68  判断对错：在Python中可以使用 for 作为变量名。0
2.69  判断对错：在Python中可以使用 id 作为变量名，但是不建议这样做。1
2.70  判断对错：一个数字5也是合法的Python表达式。1
2.71  判断对错：执行语句from math import sin之后，可以直接使用sin()函数，例如 sin(3)。1
2.72  判断对错：一般来说，Python扩展库没有通用于所有版本Python的，安装时应选择与已安装Python的版本对应的扩展库。1
2.73  判断对错：Python变量名区分大小写，所以student和Student不是同一个变量。1
2.74  判断对错：在Python 3.x中reduce()是内置函数。0
2.75  判断对错：如果只需要math模块中的sin()函数，建议使用from math import sin来导入，而不要使用import math导入整个模块。1
2.76  判断对错：表达式pow(3, 2) == 3 ** 2的值为True。1
2.77  判断对错：已知x = 3，那么执行语句x+=6之后，x的内存地址不变。0
2.78  判断对错：安装Python扩展库时只能使用pip工具在线安装，如果安装不成功就没有别的办法了。0